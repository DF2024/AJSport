import * as React from 'react';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';

function ButtonClasic({name}) {

    return(
        <Button variant="contained">{name}</Button>
        
    )

}

export default ButtonClasic